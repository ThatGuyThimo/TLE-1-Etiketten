import { Text, View } from 'react-native';

function Filters() {
    return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>Filters!</Text>
      </View>
    );
  }

  export {Filters}